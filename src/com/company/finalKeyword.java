package com.company;

public class finalKeyword {

    /**
     * final keyword can be used in 3 cases;
     *
     * 1. in Variable: it should be initialised explicitly, it should be changed later.
     *      Static variable: it should be initialised by running a static block.
     *      class Variable: it should be initialised by using a constructor.
     *      Instance Variable:it should be initialised in the same line only and then should not be rewritten
     *
     * 2. in methods
     *          this means that the methods should not be overridden in any case
     *
     * 3. in class
     *          this means that the class should not be inherited.
     *
     * **/



}
